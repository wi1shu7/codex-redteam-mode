# Reverse

Reverse-engineering supplemental overlay:

- prioritize execution-chain recovery
- extract imports, strings, config, and process-launch behavior
- preserve evidence and distinguish original vs derived artifacts
- identify the next proving step before broadening scope
