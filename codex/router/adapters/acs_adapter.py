from __future__ import annotations


def anthro_pack_notes() -> dict[str, str]:
    return {
        "pack-layout": "Adopt lightweight SKILL.md domain cards as the single per-skill runtime source.",
        "progressive-disclosure": "Do not front-load large doctrine blocks; select the pack first, then keep runtime context compact.",
        "phase-packs": "Treat attack, reverse, audit and evasion as dedicated skill-pack families rather than one monolithic prompt.",
    }
